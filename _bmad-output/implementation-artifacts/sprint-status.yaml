# generated: 2026-06-02
# last_updated: 2026-07-02 (4-0 → done via bmad-code-review ultracode + fix-everything: 3 decision-needed [DN-1 stat-text category mis-classification, DN-2 coverage-gate hardening, DN-3 null-primary hybrid promotion] + 2 patches [P-4 counts, P-5 INERT rationale] all resolved; re-ran generate_item_db.py vs live PoB4LE dev → 508→603 statKey-resolved 45.5%→54.0%, 514 unmapped; re-verified cargo scoring-core 136 + ehp_reference 4/4 [EHP ±2% parity unperturbed] + lebo 43 incl. both gear_affix_parity + no_dead_gear_stat_keys, check_coverage GATE PASSED 9/9 semantic pins, pnpm build clean, pnpm vitest 1278/7 exact standing baseline. Deferred #32/#33/#34/#35 latent robustness → deferred-work.md.)
# prior_2_7: 2026-06-05 (2-7 → review via dev-story: idol editor tray + grid (FR-38, UX-DR11; visual + interaction rebuild only, idol data wiring unchanged from Phase 3). New IdolEditor container lifts placing state (selectedTypeId/affixConfig/hoverCell) + owns placeIdol/clearIdolSlot/resetIdolGrid; new IdolTray (one button-card per idolData.idolTypes: proportional rows×cols shape box + W×H label, displayName, derived affix descriptor, count chip, name filter; aria-pressed selection → gold; selected card reveals existing IdolAffixPicker in EDIT mode — no Place/Cancel — wired to container setters + requires-both hint); IdolGrid refactored from pendingCell size-<select>/configuringNew flow → props-driven preview/place/remove (data-driven gridConfig.rows/cols, blocked cells aria-disabled non-button, footprint preview via getCellsForPlacement+validatePlacement, occupied head-cell spans gridColumn/gridRow → clearIdolSlot, invalid cells non-clickable); Active Idol Stats summary (placed affix displayName+T{tier}, empty state); IdolTab max-w-3xl→max-w-5xl hosts <IdolEditor/>, guards preserved; outer onClick deselect + pane stopPropagation. In-place affix edit DROPPED for place/remove (Alec Option A, documented trade). ContextPanel (dead consumer) <IdolGrid/>→<IdolEditor/> compile-fix only. IdolGrid.test.tsx rewritten to tray-driven contract (42 tests: loading guard, data-driven grid + 5 blocked aria-disabled, card-per-type W×H+name, filter narrows, select→aria-pressed+picker, configure prefix→valid cell→placeIdol shape, requiresBoth-only-prefix blocked+hint, requiresBoth both→places, hover footprint preview, occupied→clearIdolSlot, outside-click deselect, Active Idol Stats text+empty, Reset all idols, axe×2); 1 test-only fix (getByText→getAllByText for dual W×H surface, no source defect). Source Audit: N/A — no-new-stat/no-dead-key (descriptor + summary re-present shipped idolData affix data; placed idols produced by placeIdol AND consumed by toIdolPlacements/compute_stats). tsc 0 / build 0 / full suite 1125 passed, 8 failed across exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls) — no new failures, no baseline cleared/added. Scope boundary clean: idol data wiring (placeIdol/clearIdolSlot/resetIdolGrid/updateIdolAffix/validatePlacement/getOccupantAt/getCellsForPlacement/idolData/PlacedIdol/toIdolPlacements/compute_stats), CenterCanvas Idols badge/routing (2.5), Blessings/Conditions (2.6), RightPanel/LeftPanel/AppHeader/StatusBar, stat sheet/optimizer/gear, and any store/dep/IPC/DnD/router untouched.)
# prior_2_6: 2026-06-05 (2-6 → review via dev-story: blessing editor card grid (FR-37, visual rebuild only). BlessingsPanel rebuilt per-timeline dropdown+search → two-column card grid (gridTemplateColumns 1fr 1fr); one BlessingCard per timeline (12 timelines × 4), header gold when active; inline <button> OptionRows incl. leading None row preserving setBlessing(timelineId,null) clear path; active row gold border+gold-soft text+bg-hover tint + aria-pressed (color-independent selection), active card gold-dim border; per-row statEffects summary via extracted formatStatEffects() (existing data re-presented, no parse change); removed useState/search/<select>; fixed undefined var(--color-border)→--color-bg-elevated; Dismiss now uses global :focus-visible ring. BlessingTab Blessings column max-w-2xl→max-w-[880px], Conditions + !activeBuild guard untouched. BlessingsPanel.test.tsx rewritten to card-grid contract (13 tests: no combobox, card-per-timeline, inline rows + None, setBlessing(id|null), aria-pressed gold active, statEffects text, staleness, axe). Source Audit: N/A — no-new-stat/no-dead-key (statEffects is shipped blessings.json data re-presented; blessings produced by setBlessing AND consumed by snapshot serializer). tsc 0 / build 0 / full suite 1120 passed, 8 failed across exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls) — no new failures, no baseline cleared/added. Scope boundary clean: ConditionsPanel, CenterCanvas tab badge/routing (2.5), RightPanel/LeftPanel/AppHeader/StatusBar, blessing data wiring (setBlessing/gameDataStore/blessings.json/snapshot/compute_stats), stat sheet/optimizer/gear/idols, and all stores/deps/IPC untouched.)
# project: LEBOv2
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-06-02
last_updated_dev_41: 2026-07-02 (Story 4.1 → review via dev-story: affix prefix/suffix `position` discriminator — thin TS layer over 4.0's already-built corpus+Rust discriminator (verified at HEAD, NOT re-implemented). AC1: added optional position?:'prefix'|'suffix' to AffixEntryV2 (build.ts, absent→prefix AR-2); sourced it from r.affixEntry.type in GearSlot.buildAffixEntries ('implicit'→prefix D3); the two typeless sites (GearInput free-text, buildPersistence v1→v2 migration) left defaulting to prefix. AC2 (the REAL AR-15 fix): buildSnapshotSerializer.toGearSlots now partitions valid affixes — position==='suffix'→suffixes[], else→prefixes[] — deleting the hard-coded suffixes:[] + "all to prefixes" comment; Rust consumers already chain both vecs so NO engine change. AC3: re-exported GearSlotRanking+WishlistAffix from scoring-core lib.rs (both already pub). Task 5 verify-only CONFIRMED: GearAffixData.affix_class is the AffixPosition enum (deferred #127 "String" STALE), openrouter_service.rs:484 already asserts MODELS.len()==7, affixes.json ships type on all 1,117 (840 prefix/277 suffix) — no Rust engine/loader/data/MODELS-test change. TRAPS avoided: no Rust AffixEntry.position field (D1), corpus field kept `type` mapped→`position` at the FE boundary (D2), no affixes.json/generate_item_db.py edit, no detect_mismatched_affixes/affix_scope (4.2). Tests (verification guardrail = ROUTING not counts): replaced the bug-encoding suffixes===[] serializer assertion with prefix/suffix routing over real corpus ids (affix-inevitable-prefix→prefixes, affix-of-defense-suffix→suffixes) + back-compat (absent→prefixes never suffixes); +2 GearSlot component tests proving type→position sourcing (implicit→prefix; picker affix-speed suffix→suffix). pnpm build clean; full pnpm vitest 1281 passed / 7 failed = EXACT standing baseline (ProviderSelector×5/Settings×1/TreeControls×1), +3 tests, no new failures, no baseline cleared/added; cargo scoring-core 136 + lebo 43 (frozen gear_affix_parity_*/no_dead_gear_stat_keys green) + ehp_reference 4/4 (EHP ±2% parity unperturbed). Scope fence held: no compute math, no build_snapshot.rs/game_data*.rs/modifier.rs/gear.rs/synergy.rs/openrouter_service.rs, no store/view/router/Tauri-command, no new dependency. D4: proceeded from HEAD per Alec. epic-4 stays in-progress (4-0 done, 4-1 → review; 4-2..4-6 unblock once both data gates land). Recommend code-review with a DIFFERENT LLM.)
last_updated_create_41: 2026-07-02 (Story 4.1 → ready-for-dev via create-story + ultracode: affix prefix/suffix `position` discriminator — Epic 4's 2nd data gate. GROUND TRUTH (6 parallel HEAD-verifiers, cross-corroborated + 6 highest-risk claims re-verified inline): Story 4.0 already built the discriminator on the corpus+Rust side, so 4.1 NARROWED to a thin TS layer + 1-line lib.rs housekeeping. DONE-by-4.0 (verify, DON'T redo): affixes.json ships `type`:'prefix'|'suffix' on all 1,117 (840/277, 0 missing); GearAffixData.affix_class is the AffixPosition ENUM (game_data.rs:23 — deferred#127 'String' is STALE) populated by loader from corpus `type` (game_data_loader.rs:225-229,257); GearSlotSnapshot splits prefixes/suffixes vecs (build_snapshot.rs:13-21) + ALL consumers chain both (compute/mod.rs:198, gear.rs:234/255, synergy.rs:221 — detect_mismatched_affixes is SCOPE-based NOT position-based, inert until affix_scope populated=4.2); AffixPosition enum+from_data_str+#[default]Prefix (modifier.rs:188-205); MODELS.len() test already ==7 (openrouter_service.rs:484 — NOT scoring-core/game_data_loader). PENDING (this story): add `position?:'prefix'|'suffix'` to AffixEntryV2 (build.ts:9-16); source it from the in-scope-but-dropped r.affixEntry.type in GearSlot.buildAffixEntries (GearSlot.tsx:51-60, map 'implicit'→prefix); route prefix/suffix into the EXISTING suffixes[] in buildSnapshotSerializer.toGearSlots (buildSnapshotSerializer.ts:155-171 forces prefixes + hardcodes suffixes:[] = the REAL live AR-15, NOT a Rust bug); re-export GearSlotRanking/WishlistAffix from lib.rs:26-29 (both already pub stat_sheet.rs:161/170). 4 TRAPS flagged: D1 do NOT add position to Rust build_snapshot::AffixEntry (redundant w/ vec split → desync); D2 keep corpus field `type`, map→build `position` (a deserializer keyed on 'position' over 'type'-named data = silent all-prefix = re-introduces AR-15); MODELS test already fixed; #127 String→enum already done. Source Audit = data-sourced DISCRIMINATOR (not a compute StatKey/StatSheet field, so no value+element compute parity applies): real chain ModItem.type→affixes.json→RawAffix.affix_type→load_item_database→AffixEntry.type→AffixEntryV2.position→serializer→vecs→consumers; produced+consumed (no dead field); honest prefix-default for free-text/migrated; tests assert ROUTING over real corpus ids (affix-inevitable-prefix→prefixes, affix-of-defense-suffix→suffixes), NOT counts, and REPLACE the bug-encoding suffixes===[] assertion. Adversarial-validation subagents blocked by spend limit → re-verified inline vs HEAD instead, all CONFIRMED. GIT: HEAD 9137ad6 is a raw [AutoSave]; 4.0's data half is AutoSave-only (7730e4b/9137ad6) — recommend committing a reviewed 4.0 baseline before 4.1 (memory autosave-watcher-unvalidated). epic-4 already in-progress (set by 4.0), no epic change. Standing test baseline: ProviderSelector×5/Settings×1/TreeControls×1.)
last_updated_dev_40: 2026-07-02 (Story 4.0 → review via dev-story: affix stat-semantics data gate IMPLEMENTED. generate_item_db.py rewritten (group ModItem by affix INDEX → 1,112 real affixes not 4,176 name-fragmented; derive statKey/modifierType/scope/damageType from STAT TEXT not name; hybrids' 2nd stat → extraStats[]; itemSlots AUTHORED from a committed stat-category→gear.rs-slot table — Decision D2, NO affix→slot source exists in PoB4LE [verified ModItem/bases/LEToolsImport/ModCache] and external LE-tools/tunklab spiked+rejected as unjoinable/fragile; uniques inline-parsed + base implicits populated — D3 full). New committed check_coverage.py + affix-manifest.json (AC2): 1,117 affixes / 508 statKey (45.5%) / 508 picker-visible ALL clean (0 missing slots/key/unnamed) / 55 statKeys 0 dead / 2,338 unique mods 0 dangling / 821-of-897 bases w/ implicits / 30 deferral classes. D4: the ≥95%-of-total AC gate is INFEASIBLE (engine models ~55 consumable stats; ~54% of the LE corpus is unmodeled mechanics/inert keys — emitting them = dead keys) → reframed to picker-visible-100%-clean + real counts + 40% regression floor, unmapped 570 listed in manifest; SURFACED to Alec non-blocking. Rust: GearAffixData → MULTI-STAT (GearAffixStat + item_slots); compute/mod.rs gear loop iterates stat clauses so HYBRIDS contribute both (Alec-authorized engine change past the story's loop fence); game_data_loader parse_gear_affixes populates gear_affixes (mirrors idol path, graceful-degrade) + stat_key_from_str extended to full vocabulary (single idol+gear resolver, D1); RawAffix += stat_key/extraStats (additive, item-search untouched); gear.rs score_slot slot-validity filter → per-slot wishlists DIFFER (AC3). Tests (AC4): affix-inevitable-prefix t1 → exactly 4.0 VoidPenetration @ weapon:affix-inevitable-prefix; affix-glacial-prefix → 30.0 FreezeRate + 5.0 ColdRes (hybrid); no_dead_gear_stat_keys (every emitted StatKey moves a field) — this guard CAUGHT 13 engine-inert keys now excluded (added_*_damage×5 [FlatAdded* unqueried], max_mana/mana_regen/cooldown [unqueried], increased_minion_count/hp [compute_minion hardcodes 0.0], stun_chance, increased-crit [offense.rs Flat-crit-only]). annotate_s4_affixes.py DELETED (superseded). cargo scoring-core 136 pass + tests/ehp_reference.rs 4/4 (EHP ±2% parity gate UNPERTURBED by multi-stat) + lebo 43 pass; pnpm build clean; full pnpm vitest 1278 passed / 7 failed = EXACT standing baseline (ProviderSelector×5/Settings×1/TreeControls×1), no new failures. Scope fence held: no compute math beyond gear loop, no serializer/AffixEntry/lib.rs (4.1), no UI/store, no new command; BuildScore→tunklab-EHP re-baseline stays deferred. FLAGGED engine gaps (NOT 4.0): flat added base damage unmodeled, increased-crit not consumed, minion count/hp + mana hardcoded/uncomputed — each an engine-model follow-up. Recommend code-review with a DIFFERENT LLM.)
last_updated_create_40: 2026-07-02 (Story 4.0 → ready-for-dev via create-story: affix stat-semantics data gate — the Epic 4 data gate + FIRST Epic 4 story, so epic-4 backlog→in-progress. GROUND TRUTH (2 parallel code-audit agents, corroborated at HEAD): the scoring engine is ALREADY built + correct — GearAffixData (scoring-core game_data.rs:8-23), the build_registry gear loop (compute/mod.rs:197-207), and gear.rs run_gear_scoring all consume real per-tier stat values; the ONLY reason gear scores 0 is DATA. affixes.json (4,176 entries) carries 0/4,176 statKey, constant modifierType:"increased", itemSlots empty on 4,171, ~86% unnamed-*; and TWO DISJOINT paths never join — item_data_service.rs:41-55 loads affixes.json into the item-search ItemDatabase (RawAffix, no statKey) while game_data_loader.rs:184 hardcodes gear_affixes:HashMap::new() (never populated) → equipped affixes silently dropped at compute/mod.rs:199 (zero stat contribution, all 12 slot wishlists empty). SCOPE: (1) extend docs/data-transform/generate_item_db.py (transform_affixes :184-233 discards stat text at :214-215/:225-231) to RETAIN stat text → emit snake_case statKey + real modifierType + tiers + name + itemSlots as a committed re-runnable script; supersede heuristic annotate_s4_affixes.py; (2) committed coverage gate + manifest (≥95% statKey, no unnamed-*/itemSlots-non-empty for picker-visible, 100% unique-ref resolution, implicits populated-or-deferred, actual counts); (3) populate GameData.gear_affixes in game_data_loader.rs mirroring the idol path (:123-139 + stat_key_from_str :519-557 + tier-avg :128-132), add stat_key:Option<String> to RawAffix; (4) add item_slots to GearAffixData + gear.rs score_slot per-slot filter (:132-189); (5) end-to-end value+element parity test + source attribution + no-dead-StatKey guard. FULL non-N/A Source Audit (THE guardrail story — the corpus-scale computed-but-not-sourced case). SCOPE FENCE: position/serializer emit/lib.rs re-exports = Story 4.1 (NOT 4.0); stale MODELS.len() already fixed; no UI/store/compute-math change; BuildScore→tunklab-EHP re-baseline is a flagged SEPARATE deferred story (gear weights ride legacy effective_hp). 4 open decisions surfaced: D1 snake_case statKey resolver [recommended]; D2 itemSlots SOURCE = biggest risk, spike PoB4LE first; D3 uniques/implicits in-scope w/ manifest deferral hatch; D4 ≥95% feasibility — report real counts, adjust gate w/ Alec if lower. Standing test baseline: ProviderSelector/Settings/TreeControls (SkillTreeCanvas cleared in 3.5).)
last_updated_retro_3: 2026-07-01 (Epic 3 retrospective → DONE via bmad-retrospective + ultracode: 8-agent adversarial analysis (5 story deep-dives + AutoSave forensics + Epic 4 readiness), every claim verified vs code at HEAD — 54 CONFIRMED / 12 CONTRADICTED / 3 UNVERIFIABLE. Epic 3 = 5/5 done, truly complete + clear to start Epic 4 (uncoupled — no 3.x story touched Rust/gear). Verified at HEAD: 3.3 card numbers frontend-derived (no toNodeId→nodeEfficiencies join), fillNodeToMax budget cap Math.min(nodeSpace,budget) intact (buildStore.ts:313, survived autosave 6feafe9), removeAllPoints strict-AND orphan cascade, AR-6 store-free canvas. HEADLINE FIXES/DECISIONS: (1) AutoSave — set auto_push=false in .gitaccount (Alec) so unvalidated [AutoSave] snapshots no longer publish to origin/main; the racing writer (phantom edits to shared files) is NOT editor format-on-save (none configured) — still unidentified; pre_commit_gate.py (tsc+vitest, baseline 7) is the net; d948556 (budget cap stripped) predated the gate + is a permanent main ancestor → policy: never trust a raw [AutoSave] SHA. (2) Epic 2 retrospective WAIVED on-record + epic-2 closed → done (UI/UX lessons live in the 2.x notes). (3) Epic 4 plan corrected before create-story (epics.md callout): affix position SOURCE data already exists (affixes.json 4176 entries, 3616 prefix/560 suffix, loaded into ItemDatabase) → 4.1 is a WIRING/ingestion job not authoring; the gear stack (gear.rs / run_gear_scoring / Claude+OpenRouter narrative / Phase-3 frontend) is already wired → Epic 4 is reconcile/extend; GearAffixV2 is a phantom type (only AffixEntryV2 + GearItemV2). (4) Stand up a committed Playwright/visual-regression harness before Epic 4 (Epic 1 retro's still-open ask). Prev-retro follow-through: source-audit-in-create-story + value+element testing both APPLIED (the latter is why the stripped cap is catchable); single-stat-per-effect loader limit + stun_chance sourcing still open. Retro doc: epic-3-retro-2026-07-01.md.)
last_updated_review_35: 2026-07-01 (Story 3.5 → done via bmad-code-review: 3 adversarial layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor, all Opus 4.8) over diff c34761c..HEAD scoped to the 14 story-3.5 files. Acceptance Auditor: AC1–AC8 ALL MET; strict-.every() cascade (D3) + single-undo snapshot (AC4/Pattern P4-7) independently source-verified against live store. Triage: 1 decision-needed→PATCHED, 0 patch, 2 deferred, 3 dismissed. PATCH APPLIED (Alec approach 1): the AC2 RemoveNodeConfirmDialog was not modal vs App.tsx window-level shortcuts — Ctrl+Z/Y undo/redo + bare 1–6 tab keys fired under the open dialog (handlers guard only input targets, not an app-modal), so a mid-modal undo desynced the dialog's named orphans from confirmRemoval's recompute-at-confirm removeAllPoints (or silently no-op'd + wiped redoStack), and 1–6 unmounted the dialog with pendingRemoval still armed (phantom confirm). Fix: new appStore.isBlockingModalOpen (set while pendingRemoval open, cleared on unmount via SkillTreeView effect driven by passiveInteraction.pendingRemoval); App.tsx keydown early-returns when set (Escape/Ctrl+S still allowed); + defense-in-depth effect drops pendingRemoval on leaving the passive tab; + 3 App.keyboard.test.tsx regression tests (tab-switch suppressed, undo/redo suppressed, resume-on-close). DISMISSED: Blind HIGH "over-refund if OR-reachability" = FALSE POSITIVE (allocation gate is strict-AND .every() at buildStore.ts:211/299/369/525; the 32 real diamond nodes are genuine AND-prereqs per Task 2.4 → strict cascade correct-by-design, matches D3); Auditor "diff bundles 3.3+3.4 focus/tooltip carryover incl. optimization:* touch" = already reviewed (see last_updated_review_3334), AutoSave co-mingling not a 3.5 defect; Auditor "weaver row-h proven by outcome not call-spy" = sound/informational. DEFERRED (both LOW → deferred-work.md): (1) computeOrphanedNodes over-orphans nodes reachable only via dangling/self-loop/rootless-cycle prereq edges — unreachable in curated DAG data (strict-AND gate makes it unbuildable), realistic only via unimplemented Epic 7 import; (2) SkillTreeView orphan id→name resolution has no integration test (source verified correct). Verified: pnpm build clean (tsc+vite); full pnpm vitest 1273 passed / 7 failed = EXACT standing baseline (ProviderSelector×5 / Settings×1 / TreeControls×1), +3 tests, no regressions, no baseline cleared/added. Patch files: appStore.ts, App.tsx, SkillTreeView.tsx, App.keyboard.test.tsx. NOTE: 3-5 is Epic 3's FINAL story → 3-1..3-5 now ALL done; epic-3 left in-progress pending Alec's manual close + optional epic-3-retrospective.)
last_updated_dev_35: 2026-06-30 (Story 3.5 → review via dev-story + ultracode: shift+right-click remove-all + orphan cascade (FR-45, Pattern P4-7, NFR-6) — Epic 3 FINAL story. NEW buildStore.removeAllPoints (passive-only inverse of fillNodeToMax: ONE undo snapshot via the P4-7 tail :318-319, node + ALL transitive orphans deleted in one set(), redo cleared, zero-key delete, NO auto-create, no-op-BEFORE-snapshot on unallocated [AC6], NO isPersisted flip [passive convention]; ApplyNodeResult += removed?[]). NEW pure computeOrphanedNodes.ts — STRICT .every() root-reachability flood-fill [D3: a diamond child that lost a REQUIRED prereq refunds out], O(V+E) adjacency-once, cycle-safe head-cursor+valid-set. GESTURE [D2, Alec]: shift+right rides the EXISTING onNodeClick(id,2,shiftKey) exactly like 3.4 shift+left — only canvas change is forwarding e.shiftKey at pixiRenderer.ts:619 + SkillTreeCanvas.tsx:360 [a11y onContextMenu, AC8]; PLAIN right-click UNCHANGED [remove-one + blocked-flash, AC3]; passive-guarded slotId===undefined → skill/weaver shift+right fall through to single decrement [AC5, verified row-h]; onNodeContextMenu/NodeContextMenu.tsx left INERT/untouched. NEW RemoveNodeConfirmDialog (mirrors DeleteConfirmDialog PATTERN, NO cross-feature import; AC2-literal 'Removing this node will also deallocate: <names>. Continue?'), mounted in SkillTreeView passive fragment, orphan ids→names via getNodeName(id,gameData,classId,masteryId) [treeData has no name field]. DATA CHECK (2.4): diamonds are REAL in shipped JSON — 32 >1-prereq nodes across all 5 classes (20 sentinel; 3 each acolyte/mage/primalist/rogue), e.g. void-knight-passive-void-sword requires rift-strike AND consume-void → strict cascade is load-bearing, not synthetic-only. Source Audit: N/A — no-new-stat/no-dead-key (removeAllPoints deletes keys from the same nodeAllocations applyNodeChange/fillNodeToMax write and toBuildSnapshot→compute_stats consumes; dialog re-presents shipped GameNode.name via already-sourced getNodeName). Tests value+element (exact surviving map, removed-set, undoStack===1, redo cleared, one-undo restores node+orphans, STRICT diamond b1/b2 split, gesture routing incl. weaver row-h no-removeAllPoints, orphan-name set-equality, SkillTreeCanvas.test.tsx:120 reworked to onNodeClick(id,2,shiftKey) forwarding both true+false), NOT count/snapshot. tsc/vite build clean (one mid-impl as-const→:TreeNode fixup); full pnpm vitest 1270 passed / 7 failed = SkillTreeCanvas CLEARED from the standing baseline → new baseline ProviderSelector×5/Settings×1/TreeControls×1, +33 tests, no new failures. AutoSave-anomaly re-verified on the WORKING TREE: passive guard slotId===undefined (useSkillTree.ts:72,84) + fillNodeToMax budget cap Math.min(nodeSpace,budget) (buildStore.ts:313) BOTH intact. shared→feature import (buildStore→computeOrphanedNodes) mirrors precedent useOptimizationStream→scoringEngine. ULTRACODE: parallel data-check scout + 4-lens adversarial panel (acceptance/scope-fence/algorithm/test-quality) with per-finding refutation — 1 raw, 0 CONFIRMED / 1 refuted; AC1-8 met, scope fence held, algorithm correct on chains/diamonds/multi-hop/cycles/partial builds. epic-3 stays in-progress: 3-1..3-4 done, 3-5 → review = Epic 3 FINAL story (epic-3-retrospective optional/next). Recommend code-review with a DIFFERENT LLM; flag D2 gesture refinement (right=one, shift+right=all) as NOT-a-miss.)
last_updated_create_35: 2026-06-30 (Story 3.5 → ready-for-dev via create-story + ultracode: right-click remove-all + orphan cascade (FR-45, Pattern P4-7, NFR-6) — Epic 3 FINAL story, partner to 3.4's fillNodeToMax. GROUND TRUTH (7-agent scout + 3-agent adversarial verify): buildStore.removeAllPoints is ABSENT — author from scratch mirroring fillNodeToMax (buildStore.ts:253-321) incl. its single-undo tail :318-319 + zero-key delete :235-240 + MAX_UNDO_STACK :8; the right-click→menu seam is wired-but-INERT (onNodeContextMenu typed types.ts:11,60 + threaded SkillTreeCanvas.tsx:24,34,89 + bound SkillTreeView.tsx:854/929/645 but NEVER fired; pixiRenderer.ts:619 + SkillTreeCanvas.tsx:360 both fire onNodeClick(id,2)→applyNodeChange(-1) single-decrement today; NodeContextMenu.tsx is real-but-orphaned; SkillTreeCanvas.test.tsx:120 is a standing-baseline RED test awaiting the menu path). WORK: (1) NEW pure computeOrphanedNodes.ts — strict .every() root-reachability flood-fill, O(V+E), correct on diamonds (chains cascade transitively; a diamond child that lost a REQUIRED prereq is orphaned); prereqs from treeData.edges (fromId=prereq,toId=dependent), root=indegree 0, allocated=(alloc??0)>0. (2) NEW removeAllPoints(nodeId,treeData): one undo snapshot, delete node+orphans in ONE set(), NO auto-create, no-op-before-snapshot on unallocated, NO isPersisted flip; extend ApplyNodeResult(build.ts:81) with removed?:string[]. (3) GESTURE (Alec-confirmed D2): right-click = remove ONE (unchanged applyNodeChange(-1), AC3); SHIFT+right-click = remove ALL — add a shift+right branch to the EXISTING handleNodeClick (mirrors 3.4 shift+left fill), passive-guarded (slotId===undefined); the only canvas change is forwarding e.shiftKey on the button===2 onNodeClick (pixiRenderer.ts:619 + SkillTreeCanvas.tsx:360). (4) NEW RemoveNodeConfirmDialog (centered Headless UI modal, mirror DeleteConfirmDialog PATTERN, no cross-feature import); orphan names via getNodeName(id,gameData,classId,masteryId) resolved in SkillTreeView (gameData:84/selectedClassId:88/selectedMasteryId:89). NO weaver regression: the gesture rides onNodeClick (not onNodeContextMenu), so the 3-instance shared-renderer trap (passive/skill/weaver ALL fire the shared callback; passive+weaver both slotId===undefined) is MOOT — weaver/skill shift+right fall through to their normal single-decrement; onNodeContextMenu + NodeContextMenu.tsx left INERT/untouched. Source Audit: N/A — no-new-stat/no-dead-key (removeAllPoints deletes keys from nodeAllocations, the same field applyNodeChange/fillNodeToMax produce AND toBuildSnapshot→compute_stats consumes; prompt re-presents shipped GameNode.name via already-sourced getNodeName — no new sourced value). Tests value+element (exact surviving map, removed-set, undoStack===1, diamond strict b1/b2 split, gesture-routing incl. weaver shift+right guard, SkillTreeCanvas.test.tsx:120 reworked to the onNodeClick(id,2,shiftKey) contract), NOT golden-count. DECISIONS: D1 RESOLVED (direct gesture, no menu — NodeContextMenu stays inert scaffold); D2 RESOLVED (Alec: right-click=remove-ONE preserved, SHIFT+right-click=remove-ALL — mirrors 3.4 shift+left=fill; an intentional refinement of FR-45's literal "right-click" — flag for code-review as not-a-miss); D3 RESOLVED (Alec): strict .every() — if C requires {A,B} and B is removed, C's points refund out (cascade); OR rule rejected. Confirmation (AC2) is MANDATORY whenever the cascade removes ANY other node (orphans.length>0) — primary misclick safeguard; single-step undo is the secondary backstop. Scope fence: scoring-core/Rust/compute_stats/NodeEfficiency/optimization:*, fillNodeToMax, applyNodeChange's block, skill/weaver allocation actions, isPersisted omission all untouched. Baseline after 3.3+3.4 = 1237 passed/8 failed (ProviderSelector/Settings/SkillTreeCanvas/TreeControls); fixing SkillTreeCanvas.test.tsx:120 may clear SkillTreeCanvas from baseline — verify. epic-3 stays in-progress: 3-5 is the LAST story, 3-1..3-4 done.)
last_updated_create_34: 2026-06-29 (Story 3.4→ ready-for-dev via create-story: shift+click fill node to max (FR-44, NFR-6, Pattern P4-7). KEY FINDING — the feature is ALREADY IMPLEMENTED end-to-end since commit 96d809a / 2026-05-27 (Phase-3-era, pre-dates epics.md 2026-06-02): buildStore.applyNodeChangeBulk(nodeId, treeData) (budget-limited, SINGLE undo snapshot, prereq-checked), wired through useSkillTree.handleNodeClick (button===0 && shiftKey && slotId===undefined → bulk; passive-tree-only guard), pixiRenderer.ts:627 passes e.shiftKey via onNodeClick, types.ts:8,55 carry shiftKey, and 7 store tests (buildStore.test.ts:502-571). So 3.4 is a RECONCILE + VERIFY + HARDEN, NOT a build-from-scratch — DO NOT create a duplicate bulk-allocate action (the #1 reinvention trap this story guards). Work: (1) RENAME applyNodeChangeBulk → fillNodeToMax to match AC2's literal reference + architecture Pattern P4-7 (architecture.md:394-398,531) and pair with 3.5's removeAllPoints — internal name, ONE production caller (useSkillTree.ts), low-risk; keep the (nodeId, treeData) signature (treeData needed for maxPoints/prereqs; architecture's fillNodeToMax(nodeId) is shorthand). (2) Add the EXACT AC2 value test the suite lacks: level 6 + budgetEnforced + root pre-alloc 2 (3 remaining, 2 unspent) → fillNodeToMax → root===4 (exactly 2 added) + undoStack length 1. (3) Add the passive-only shift-routing test the hook suite lacks (useSkillTree.test.ts has NO shift+click coverage): slotId===undefined → bulk fills to 5; defined slot → single applySkillNodeChange (no bulk). (4) Confirm event plumbing intact (pixiRenderer shiftKey, types) — verify-only. DECISIONS (flag for Alec): budget cap stays budgetEnforced-gated (consistent with applyNodeChange:212; default unenforced fills to maxPoints); the passive isPersisted dirty-flag omission is shared by single + bulk passive clicks → out-of-scope, DON'T fix on the bulk path alone (would make passive clicks inconsistent). Source Audit verdict: N/A — no-new-stat / no-dead-key (writes existing nodeAllocations field, consumed by toBuildSnapshot→compute_stats; value+element satisfied by the exact-allocation tests, no parse/golden-count). Scope fence: right-click remove-all + orphan cascade + NodeContextMenu + button===2 branch = Story 3.5; scoring-core/Rust/compute_stats untouched. Baseline: full pnpm vitest no new failures vs ProviderSelector/Settings/SkillTreeCanvas/TreeControls (3.3 left it at 1230 passed / 8 failed). epic-3 already in-progress (3-1/3-2 done) — no epic status change.)
last_updated_dev_34: 2026-06-29 (Story 3.4 → review via dev-story + ultracode: RECONCILE+VERIFY+HARDEN of the already-shipped shift+click fill-to-max. (1) Renamed buildStore.applyNodeChangeBulk → fillNodeToMax (interface+impl + sole hook caller useSkillTree.ts selector/call/dep/local + test describe/8 calls; BODY UNCHANGED — not a second bulk action; grep→0 hits) to match AC2's literal reference + architecture Pattern P4-7. (2) Added the missing AC2 value test: level 6 (calculatePassivePoints(6)=4), budgetEnforced, root pre-alloc 2 → fillNodeToMax → root 4 (exactly +2, budget-limited, NOT the 3 of node space) + undoStack length 1 (single step). (3) Added 3 passive-only shift-routing hook tests (suite had ZERO shift+click coverage): passive shift → fill to maxPoints 5 in one action + undoStack 1; skill-slot guard → single +1 via applySkillNodeChange (slotId===undefined gates bulk; test seeds a build first since applySkillNodeChange does not auto-create); budget-limited shift through the hook → 4. (4) Plumbing (pixiRenderer shiftKey / types.ts) + 3.5 seam (button===2 / NodeContextMenu / handleNodeContextMenu / removeAllPoints) verify-only, untouched. DECISIONS confirmed: budget cap stays budgetEnforced-gated; treeData kept in signature; isPersisted:false omission left consistent with applyNodeChange (NOT fixed on the bulk path alone). Source Audit: N/A — no-new-stat/no-dead-key (writes existing nodeAllocations, consumed by toBuildSnapshot→compute_stats; value+element via exact-allocation tests). tsc+vite clean; full pnpm vitest 1234 passed / 8 failed = exact standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls), +4 tests, no new failures. ULTRACODE: ground-truth scout (baseline + live-code claims + AC2 math, 0 discrepancies) + 3-lens adversarial panel (acceptance/scope-fence/test-quality) ALL PASS, 0 refutations, 0 scope violations. ⚠️ ENV ANOMALY (flagged for Alec): a racing external process made transient phantom edits to the two shared files stripping the passive-only guard (useSkillTree.ts:64 `&& slotId === undefined`) and the budget cap (buildStore.ts:308 Math.min(nodeSpace,budget)→nodeSpace); the AutoSave hook committed one such state as d948556 (HEAD) → that commit's buildStore.ts has the budget cap STRIPPED, while the WORKING TREE is correct (re-verified Read+grep+green test re-run). Recommend committing the verified working tree to overwrite the bad autosave + investigating the racing formatter/agent. epic-3 stays in-progress (3-3 review, 3-5 backlog).)
last_updated_create_33: 2026-06-29 (Story 3.3 → ready-for-dev via create-story + ultracode: suggestion-card content (FR-19) + card↔tree cross-highlight/compact tooltip (FR-18) + new SkillTreeCanvasHandle.focusNode(nodeId) (AR-6). FRONTEND INTEGRATION ONLY — no scoring-core/Rust, no NodeEfficiency schema, no optimization:* emit, no run_optimization payload change. AC1: add a single composite score delta (ScoreGauge round-avg-of-non-null-axes formula over the suggestion's own baselineScore→previewScore, single-node-consistent), a "N pts / M pts to reach" line (point cost = GameNode.pointCost/maxPoints; path cost = NEW frontend BFS over GameNode.prerequisiteNodeIds summing pointCost to nearest allocated — mirrors nearestAllocatedPath), and make the Claude explanation always-visible (today gated behind hover/expand at SuggestionCard.tsx:104). AC2: card hover OR click OR keyboard-focus → intensified highlight that COMPOSES with (does not clobber) the 3.2 gold/silver tier treatment — fix the deferred precedence bug by removing isGlowing from the mutually-exclusive base-draw chain (pixiRenderer.ts:453) and adding an ADDITIVE emphasis pass at the node's effective/tier-scaled radius + a reduced-motion-gated emphasisPulse ticker cloning goldPulseTick (torn down in destroy()); compact canvas tooltip (extend NodeTooltip) shows node name + point cost + path cost + per-stat deltas via computeStatDeltas(statSheet, previewStatSheet) FILTERED to changed non-inert fields. AC3: focusNode added to RendererInstance/SkillTreeCanvasHandle/useImperativeHandle, mirrors fitToTree centering with a triggerFlash-style self-removing tween — but reduced motion JUMPS to centered (must NOT copy triggerFlash's reduced-motion early-return no-op); right-panel→canvas via new optimizationStore.focusNodeId+nonce read by SkillTreeView (forces passive tab active, calls passiveCanvasRef.focusNode), canvas stays props/ref-only/store-free (AR-6). Extend (don't rewrite) SuggestionsList.handleHoverEnter — preserve the Story-4.5 preview compute_stats IPC + previewAbortRef cancel + isSyntheticNodeId guard; keep previewSuggestionRank preview distinct. New shared pure helpers (pathPointCost + extracted getNodeName/computeStatDeltas) in shared/utils/ — cross-feature imports forbidden. Source Audit: N/A — no-new-stat/no-dead-key (newStatIntroduced=false, 2 adversarial skeptics agree on every field). THE FLAGGED TRAP both skeptics surfaced: the score delta + path cost are reachable via a toNodeId→nodeEfficiencies join keyed on the CLAUDE-ECHOED to_node_id (claude_service.rs:55/scoring_commands.rs:402, can miss → empty) AND full-path vs single-node (would contradict the per-axis pills) = the penetration/stun/minion displayed-but-not-sourced defect class → story MANDATES frontend derivation from baselineScore/previewScore + gameData prereq BFS, NOT the join; per-stat tooltip excludes perpetual ±0 inert stats. Tests value+element (exact composite delta, exact path-cost integer, glowing+tier composition [currently untested — all tier tests use emptyHl()], focusNode handle-forwarding + reduced-motion jump, SkillTreeCanvas.test.tsx mock + NodeTooltip compact variant) — NOT count/snapshot. Scope boundary: shift-click (3.4), right-click + NodeContextMenu unrendered scaffolding (3.5), FR-25 grouped output (Epic 6), useSkillTree click handling, buildStore allocation actions, and all Rust/scoring-core untouched. tsc/build/full vitest must hold the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls = 1156 passed/8 failed; SkillTreeCanvas.test.tsx is IN that baseline — update its mockRenderer for focusNode without regressing further).)
last_updated_create_32: 2026-06-29 (Story 3.2 → ready-for-dev via create-story: enhanced suggested-node visualization on the PixiJS canvas (FR-17). RENDERING OVERHAUL ONLY — no scoring math, no Rust/scoring-core change, no NodeEfficiency schema change. Replace the current near-invisible thin-ring tier overlay (drawOverlayGold/Silver/Dim, static, no scale, dim 0x444455 barely visible) with FR-17's tiered hover-free treatment: drive tiered SCALING off NodeEfficiency.tier INSIDE the main node loop (pixiRenderer.ts:348-372, in place of drawAvailable for tiered-unallocated nodes — gold r×1.4 / silver r×1.2 / dim r×1.05; a ring-on-top leaves a base node inside a big ring), split drawSuggested into tier-aware draws. Add a persistent 1.8s pulse ticker (gold only, model on iconAnimTick:282, animate a gold glow layer NOT full renderTree) reading reducedMotionEnabled each frame → AC3 static fallback (skip glow, keep scale+outline). Silver = steady ring; dim = blue outline, no anim. AC2 dashed gold path line: SOURCE-AUDIT FLAG — NodeEfficiency has NO route field (stat_sheet.rs:149-157) and the overlay emit ships only node_efficiencies (scoring_commands.rs:50-52); knapsack_solution route never reaches the canvas → MUST be frontend-derived via a NEW pure BFS helper (nearestAllocatedPath.ts) over treeData.connections/edges (treeData.ts:19,23-31) to the nearest state==='allocated' node; PixiJS v8 has no native dash (hand-segment). Add missing --color-* silver/dim tokens (Pattern P4-8; gold→--color-accent-gold #C9A84C not 0xFFD700). Preserve gating: passive-tree only, suppressed at unspentPassivePoints<=0 (effectiveNodeEfficiencies SkillTreeView:656) + showOverlay toggle. Preserve the orthogonal hover-glow system (drawSuggested via highlightedNodes.glowing = Story 3.3 cross-highlight substrate). Source Audit: AC1 tiers FULLY SOURCED (gold scan.rs:153 / silver :155 / dim :157, dim is the majority bottom-half — NOT dead; produced+consumed, no snake_case mismatch); AC2 path = NEW_DATA_DEPENDENCY (frontend graph derivation, owned by the story). DECISION (Alec-confirmed): treat GOLD + SILVER only; DIM-tier nodes render as plain available (drawAvailable) — a deliberate deviation from FR-17's literal 1.05x+blue dim clause to keep suggested-vs-available crisp; dim tier still consumed as the render-as-available signal (no dead key, no drawOverlayDim). Tests value+element (exact path endpoints + tier→scale/glow + reduced-motion glow-skip), NOT count/snapshot. OUT OF SCOPE: focusNode/card cross-highlight/tooltip (3.3), shift/right-click (3.4/3.5), any Rust change. Adversarially verified (3 skeptics, high-confidence, none refuted): dim-tier live, overlay produced+consumed end-to-end, path-line unsourced-on-backend-but-frontend-derivable. Scope boundary: scoring-core/NodeEfficiency/optimization:node-efficiencies emit, RightPanel/optimization card content (3.3), skill/weaver canvases, and all store schema untouched.)
last_updated: 2026-06-29 (Story 3.2 → done via code-review: 3-layer adversarial review (Blind Hunter / Edge Case Hunter / Acceptance Auditor, all Opus, blind+parallel). AC1/AC2/AC3 + scope + test-quality all PASS (Acceptance Auditor source-verified, 13 tool-uses). 22 raw findings → 1 decision-needed RESOLVED accept-as-is (dim-tier node in `locked` state renders drawLocked not literal-AC1 "available" — honest, not a regression, only the thin dim ring was removed; deviation ratified by Alec), 3 patches APPLIED+verified (nearestAllocatedPath.ts/overlay gate: empty-allocation short-circuit so a fresh build skips per-node BFS; queue.shift→head cursor O(n²)→O(n); nodes.has guard before isAllocated for dangling connection ids), 2 deferred (isGlowing→tier precedence owned by Story 3.3 card↔node cross-highlight; tier-overlay test-hardening — silver path / ticker-teardown / dim+locked), 13 dismissed. Headline dismissals = blind false-positives against the spec the blind layers couldn't see: "silver emits a GOLD path" is AC2-MANDATED (gold path for BOTH gold+silver tiers); "z-order under nodes" + "hop-count not Euclidean nearest" are Task-3-specified; "isDimmed hides tier" is moot (dimmed = EMPTY_SET on the passive canvas, SkillTreeView.tsx:50); gold-soft token exists (global.css:29); 0x141417 matches drawAvailable convention; dim-ring drop = the Alec-confirmed DECISION. tsc 0 / build clean / affected tests 29-29 / full vitest 1156 passed, 8 failed = EXACT standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls), no new failures. Findings written to story Review Findings; 2 defers logged to deferred-work.md. PRIOR → review via dev-story + ultracode: FR-17 suggested-node visualization overhaul (rendering only — no Rust/scoring-core, no NodeEfficiency schema, no optimization:node-efficiencies emit change). Tiered rendering folded INTO the main node loop in place of drawAvailable: gold r×1.4 + 1.8s pulsing glow (persistent ticker animating ONLY a dedicated gold-glow layer, reduced-motion-gated each frame — no per-frame full renderTree), silver r×1.2 + steady glow; DIM → plain available (Alec-confirmed; legacy drawOverlayGold/Silver/Dim thin-ring 2nd pass + overlayGraphics layer removed, no double-draw). AC2 dashed gold path line frontend-derived via new PURE nearestAllocatedPath.ts BFS over treeData.connections (NodeEfficiency carries no route field — only .tier is read; no rendered-but-not-sourced defect); gold/silver branch placed BEFORE the locked branch so locked-state suggestions (prereqs-not-yet-allocated = exactly AC2's case) get treated + a line; helper returns null when a direct prereq is already allocated (available node → no line). New --color-node-suggested-silver #C0C6D2 token; tier hex via tokenized constants (gold fixed off 0xFFD700). AC3 reduced-motion: pulse suppressed, scale+outline retained; ticker torn down in destroy(). Source Audit: AC1 fully sourced, AC2 path = the flagged NEW_DATA_DEPENDENCY, frontend-derived (sourced), not read off the feed. Scope clean: no focusNode (3.3), no shift/right-click (3.4/3.5), no store/useSkillTree change, props-only canvas, System A hover-glow (drawSuggested via highlightedNodes.glowing) preserved orthogonally for 3.3. ULTRACODE 6-lens adversarial review (Opus): AC1/AC2/AC3/scope+guardrails all PASS 0 findings; test-quality PASS (wrong-tier swap / scale-drift / dim-treated / reduced-motion-not-suppressed regressions are provably caught — value/element, no count/snapshot); render-correctness 1 MEDIUM perf finding FIXED (nearestAllocatedPath reuses renderTree's nodeMap + parent-pointer BFS instead of an O(N) map rebuild per suggestion) + 1 LOW deferred (per-frame gold-glow geometry redraw — story explicitly sanctions per-frame glow-layer animation; alpha/transform refinement is a future perf pass). tsc 0 / build 0 / full vitest 1156 passed, 8 failed = exact standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls), +13 tests, no new failures, SkillTreeCanvas not regressed. PRIOR → Story 3.1 → done via code-review: 2 decision-needed findings resolved → 2 patches applied + verified. DN-1: empty-budget notice no longer goes stale on a same-build edit — SuggestionsList gates it on a live selectUnspentPassivePoints read (showEmptyBudgetNotice), auto-hiding when points are re-allocated; the empty-state copy returns. DN-2: empty-budget copy differentiated — verbatim AC3 EMPTY_BUDGET_MESSAGE kept for the all-spent zero case (AC3 still MET), new OVER_BUDGET_MESSAGE (negative/over-allocated) + LOW_LEVEL_BUDGET_MESSAGE (level 1-2) via emptyBudgetMessage(available,unspent). 3-layer adversarial review (Blind Hunter / Edge Case Hunter / Acceptance Auditor, all Opus): all 3 ACs MET, no Critical/High, no do-not-touch boundary crossed; 4 findings dismissed [vacuous Rust off-tree assertions = documented drop-params tradeoff with genuine positive element-level coverage; untested guard seam = mitigated by buildStore.test.ts real-selector tests; selector "missing" null-fallbacks = false positive, BuildState.characterLevel/nodeAllocations are required (build.ts:45,47); payload-level AC1 = spec-sanctioned]. tsc 0 / build 0 / full vitest 1143 passed, 8 failed = exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls), +3 tests vs dev-story. PRIOR → review via dev-story: scope-restricted optimizer + empty-budget fallback (FR-15/FR-16, NFR-3, Pattern P4-3/ADR-P4-003, AR-5 partial). BACKEND (AC1/AC2): assemble_run_optimization_payload (scoring_commands.rs) filtered to passive-node knapsack suggestions only — four off-tree categories removed (critical_warning/game_changer/mismatched_affix/zero_value_allocation), each surviving suggestion tagged kind:"passive_node"; the dead top-level compute_stats+run_synergy_detection were dropped from run_optimization (scan computes its own baseline) AND the stat_sheet/synergy_flags params dropped from the assemble fn (story's PREFERRED path → off-tree leakage now structurally impossible, not just runtime-filtered); optimization:node-efficiencies overlay emit PRESERVED (Story 3.2). Floor check UNTOUCHED — useStatSheet→compute_stats→StatSheet.warnings byte-identical (AC2 = stop re-presenting warnings AS suggestions). New #[cfg(test)] mod in scoring_commands.rs (3 element-level guard tests: every emitted suggestion kind==passive_node, sequential rank-from-1, no warning:/unique:/synergy: leakage, empty-knapsack→empty inverse, find_map target-resolution vs path.last()). FRONTEND (AC3, Alec-confirmed frontend-guard): selectUnspentPassivePoints selector (buildStore, reuses calculatePassivePoints level-2 idiom), optimizationNotice+setter (optimizationStore, reset in clearSuggestions), empty-budget guard in startOptimization (unspent<=0 → verbatim EMPTY_BUDGET_MESSAGE + return BEFORE invokeCommand — no paid Claude call, no isOptimizing), informational role=status notice in SuggestionsList (data-testid empty-budget-notice, precedence over empty-state/well-optimized copy); OptimizeButton disabled logic in RightPanel untouched (stays clickable). Dead-but-keep: SuggestionsList synthetic-id/informational scaffolding (warning:/unique:/synergy:) retained for Epic 6 Complete Build Optimizer. ULTRACODE: 5-lens parallel adversarial verification (AC1/AC2/AC3/scope+regression all PASS, task-fidelity CONCERN on 2 LOW nuances); 3 LOW findings actioned (direct selectUnspentPassivePoints unit tests in buildStore.test.ts, clarifying comment on now-vacuous off-tree assertions, hardened bridge→target fixture), 1 LOW DEFERRED out-of-scope (invoke_claude_api is an orphaned second optimization:* emitter — removing it touches lib.rs invoke_handler, on this story's do-not-touch list). Source Audit: N/A — no-new-stat/no-dead-key (removes off-tree entries + adds a static budget-gated UI string; surviving knapsack kind produced AND consumed; warnings stay produced-and-consumed). tsc 0 / build 0 / cargo: scoring-core 138 pass, scoring_commands 3 new pass (lone openrouter MODELS.len()==4 fail is the documented pre-existing out-of-scope baseline) / full vitest 1140 passed, 8 failed across exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls) — no new failures, +11 tests. Scope boundary clean: scoring-core engine, compute_stats, pixiRenderer/useSkillTree (Stories 3.2/3.4/3.5), RightPanel button disabled-logic, lib.rs invoke_handler, and all store schema beyond the two additive frontend fields untouched. PRIOR → Story 2.8 → done via code-review: 1 decision resolved + 1 patch applied. Footer text brightened to clear WCAG AA — base muted→secondary (#9E9494, 6.71:1), values secondary→primary (#F0EAE0, 16.54:1), gold dot unchanged (8.66:1); preserves the dim-label/bright-value hierarchy (Alec's call: keep the look but lift to AA). Decorative ●/·/— wrapped in aria-hidden (zero visual change); 2 StatusBar.test.tsx queries updated. Acceptance Auditor PASS (AC1+AC2 MET); Edge Case Hunter found no crashes/undefined-tokens; Blind Hunter's "lost visible online indicator" was a false positive (survives in AppHeader). 5 deferred (offline-at-startup not announced [pre-existing]; unsaved-state not aria-live [sanctioned Task 2]; CLAUDE_MODEL_LABEL hand-mirror; LLM else-branch openrouter assumption; center not truly centered), 9 dismissed. tsc 0 / StatusBar 11/11 / full suite 1129 pass, 8 failed across exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls) — no new failures, no baseline cleared/added. PRIOR → 2026-06-28 Story 2.8 → review via dev-story: StatusBar.tsx rebuilt to the prototype three-segment footer — left Data {version}+date (value --color-text-secondary, date font-mono), center flex-1 unsaved-state (dirty=activeBuild&&!isPersisted → gold "● Unsaved changes"; persisted → muted "● All changes saved"; no build → empty spacer; NOT aria-live), right LLM provider+model (claude → "Claude · claude-sonnet-4-6" via CLAUDE_MODEL_LABEL const mirroring claude_service.rs:8 + drift note; openrouter → honest "OpenRouter · free rotation" [7-model per-request rotation, get_model_preference unregistered]; null → hidden). Visible Online/Offline segment dropped (dup of AppHeader 2.2) but PRESERVED as an sr-only aria-live=polite region — the app's only spoken connectivity announcer. Token remap: footer bg --color-bg-elevated→--color-bg-base, border --color-bg-hover→--color-bg-elevated, base text --color-text-secondary→--color-text-muted. No store/IPC/Rust/dependency change; Source Audit N/A (no-new-stat/no-dead-key). StatusBar.test.tsx rewritten to the three-segment contract — 11 tests (data version+date/version-only/hidden, dirty→gold, persisted, no-build, claude/openrouter/null LLM, sr-only connectivity flip, axe), reset harness extended to useBuildStore. tsc 0 / StatusBar 11/11 / full suite 1129 passed, 8 failed across exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls) — no new failures, no baseline cleared or added / build 0 (pre-existing >500kB chunk advisory only). Scope boundary clean: AppHeader/LeftPanel/RightPanel/CenterCanvas/editors (2.2-2.7) untouched, all stores read-only, no get_model_preference registration, openrouter MODELS.len()==4 Rust-test inconsistency left as documented out-of-scope.) PRIOR → 2026-06-05 (Story 2.7 → done via code-review: all 6 ACs verified MET by Acceptance Auditor; every Critical scope boundary clean — idol data wiring untouched (idolGridUtils/stores/PlacedIdol/toIdolPlacements byte-identical), no DnD/store/dep/IPC, grid fully data-driven (no hardcoded 5×4), token discipline held, ContextPanel edit is sanctioned dead-component compile-fix. 0 patches applied; 1 decision resolved keep-as-is by Alec [tray filter excluding the selected idol keeps its config panel open — persists in-progress affix config across a filter keystroke, protects user work, panel header still names selection]; 3 deferred [stale/removed idolTypeId → ghost placed idol via getOccupantAt skip (pre-existing util behavior, data-staleness, idol wiring out of scope); malformed/degenerate idol-type or grid data not defensively annotated (data-quality, Phase-3 data valid, no crash); crypto.randomUUID unguarded in handlePlace (pre-existing, available in Tauri secure-context webviews)]; 21 dismissed incl. 2 adversarial false-positives dissolved against real source [invalid-cell click "deselects" — grid wrapper IdolGrid.tsx:52 stopPropagation blocks it; "stale hover preview" — valid cells clear hoverCell on their own onMouseLeave]. Build/suite green per dev record (tsc 0 / idol 42/42 / full suite 1125 passed, 8 standing-baseline failures), not independently re-run. Prior story 2.6 → done via code-review: all 6 ACs verified MET by Acceptance Auditor, all stated scope boundaries clean (blessing data wiring/setBlessing/snapshot, ConditionsPanel, stores, other panels untouched); token discipline satisfied + pre-existing undefined `--color-border` bug confirmed fixed. Independently verified tsc 0 / BlessingsPanel 15/15 / full suite 1122 passed, 8 failed across exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls) — no new failures. 2 patches applied [None rows given timeline-scoped aria-label via new OptionRow `ariaLabel` prop — fixes 12 identical "None" accessible names; test hardening: per-card button-count assertion so a dropped-entry regression can't pass — +2 tests, 13→15], 1 decision resolved keep-as-is by Alec [single-select via aria-pressed toggle buttons not radiogroup — spec-sanctioned by AC6, axe-clean; Epic 2 pattern kept], 3 deferred [stale/orphan selectedId → invisible selection (data-wiring, out of scope); formatStatEffects mis-renders non-increased/non-flat modifiers + negative/zero values — both pre-existing verbatim format rule, latent], 10 dismissed [Blind/Edge false-positives: Dismiss focus removal is actually a fix (global :focus-visible ring applies), summary='' guard safe, empty statEffects/entries impossible-or-handled, dup timelineId deduped by memo, dup blessing-id hypothetical, search-coverage-loss by design]. Prior → review via dev-story: blessing editor card grid (FR-37, visual rebuild only). Prior story 2.5 → done via code-review. all 8 ACs verified MET by Acceptance Auditor, every stated scope boundary clean (PixiJS/pixiRenderer/stores/schema untouched); 0 patches applied, 1 deferred [LeftPanel highlights raw centerTab vs CenterCanvas's new safeCenterTab guard — unreachable today, typed setter + non-persisted store], 13 dismissed incl. the lone decision-needed resolved keep-as-is by Alec [center "Passive Tree"/key-1 no-op from a skill slot is the intended coarse passive-vs-weaver mirror per AC4] plus a batch of Blind-Hunter false-positives that dissolve against the real sync code [keyboard-test "race", lost-tab-selection, stale-effect closure, "missing" settings guard — all verified non-issues]. Prior → review via dev-story: center canvas six-tab bar with Weaver. CenterTab union += 'weaver'; six-tab bar Passive Tree | Weaver ┊ Gear | Skills | Idols | Blessings with Weaver badge (Σ weaverAllocations) + safeCenterTab guard; keyboard 1–6 remap (weaver@2, new 6→blessing); loop-safe centerTab ⟷ SkillTreeView.activeTabIndex sync (activeTabIndex stays sole canvas authority); compile-required weaver entry in getSectionStatus's exhaustive Record<CenterTab,…> with done: weaverPoints>=1; LeftPanel Weaver nav row + 'Passive Tree' relabel (the row 2.3 deferred here). New CenterCanvas.test.tsx + buildSectionStatus.test.ts; extended App.keyboard.test.tsx (1–6) + reconciled LeftPanel.test.tsx (5→6 rows, 'Passive Tree' label, dup '0 pts' via getAllByText). tsc 0 / build 0 / full suite 1119 passed, 8 failed across exactly the standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls) — no new failures, no baseline file cleared or added. Source Audit: N/A (no-new-stat/no-dead-key — Weaver badge re-presents existing weaverAllocations; excluded from snapshot). Scope boundary: PixiJS canvas, Epic-5 skill-tree relocation, context-tab content (Epics 1/3/4), RightPanel/AppHeader/StatusBar, and all store schema/types beyond the union string are untouched.)
last_updated_create_31: 2026-06-29 (Story 3.1 → ready-for-dev via create-story: scope-restricted optimizer + empty-budget fallback (FR-15/FR-16, NFR-3, Pattern P4-3/ADR-P4-003, AR-5 partial). BACKEND (AC1/AC2): filter assemble_run_optimization_payload (scoring_commands.rs:352–491) so suggestions[] is built ONLY from the knapsack passive-node category (#4 "efficiency"); drop the four off-tree categories — critical_warning (from stat_sheet.warnings), game_changer + mismatched_affix + zero_value_allocation (from synergy_flags); tag each emitted suggestion kind:"passive_node". Floor check UNTOUCHED — it still runs in compute_stats/useStatSheet and populates StatSheet.warnings (the user-facing path); AC2 = stop re-presenting warnings AS suggestions, not delete them. Now-dead top-level compute_stats + run_synergy_detection inside run_optimization may be dropped (scan computes its own baseline); KEEP the optimization:node-efficiencies overlay emit (Story 3.2). New #[cfg(test)] mod in scoring_commands.rs (currently untested) asserts element-level kind=="passive_node" + no warning:/unique:/synergy: leakage (value+element, not count). FRONTEND (AC3, recommended frontend-guard): add selectUnspentPassivePoints (buildStore, reuse the level-2 idiom at 205/296), optimizationNotice+setter on optimizationStore (cleared in clearSuggestions), guard in startOptimization (unspent<=0 → set notice + return BEFORE invokeCommand — no paid Claude call), informational (non-error) notice branch in SuggestionsList ahead of the well-optimized/empty copy; verbatim message "No unspent passive points available. Allocate additional points or use the Complete Build Optimizer for a full reallocation analysis." DECISION (Alec-confirmed): frontend-guard for AC3 (backend-guard rejected — client-known condition, avoids a wasted paid Claude call). Dead-but-keep: SuggestionsList synthetic-id/informational-card scaffolding (warning:/unique:/synergy:) becomes unreachable from run_optimization — RETAINED for Epic 6 Complete Build Optimizer (complete-opt:*/useCompleteOptStream), not a regression. Source Audit: N/A — no-new-stat/no-dead-key (removes off-tree entries + adds a static budget-gated UI string; surviving knapsack kind is produced AND consumed; warnings stay produced-and-consumed). Scope boundary: scoring-core engine, compute_stats, pixiRenderer/useSkillTree (Stories 3.2/3.4/3.5), RightPanel button disabled-logic, lib.rs invoke_handler, and all store schema beyond the two additive frontend fields are untouched.)
last_updated_create_28: 2026-06-10 (Story 2.8 → ready-for-dev via create-story: status bar (FR-39, UX-DR12). Single-component rebuild of StatusBar.tsx to the prototype's three-segment footer — left Data: {dataVersion} — {date} (existing render logic kept verbatim; shipped manifest already yields "Season 4 (Shattered Omens) — 2026-03-26"), center flex-1 unsaved segment (dirty = activeBuild && !isPersisted → gold "● Unsaved changes"; persisted → muted "● All changes saved"; no build → empty; NOT aria-live — mutation spam), right LLM segment from appStore.llmProvider (claude → "Claude · claude-sonnet-4-6" via TS constant mirroring CLAUDE_MODEL claude_service.rs:8 with drift-tripwire test; openrouter → honest "OpenRouter · free rotation" since openrouter_service rotates 7 models per request and get_model_preference is NOT a registered command — prototype's mocked gemini-2.0-flash refused per honest-display; null → segment hidden). Visible Online/Offline dropped (dup of AppHeader 2.2, prototype puts it in header) but the footer's aria-live polite connectivity announcement is preserved as an sr-only region — it is the app's only spoken online/offline announcer. Token remap: footer bg → --color-bg-base, hairline → --color-bg-elevated (no --hairline/--color-border). No store/IPC/Rust/dependency change; no new stat (Source Audit: N/A — no-new-stat/no-dead-key; all values re-present existing store state). StatusBar.test.tsx rewritten to new contract + axe. Epic 2 a11y closeout (AC2) = per-component axe tests + no new failures vs standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls). Scope boundary: AppHeader/LeftPanel/RightPanel/CenterCanvas/editors (2.2-2.7), all stores read-only, no get_model_preference registration, pre-existing openrouter MODELS.len()==4 test inconsistency left alone.)
last_updated_create_27: 2026-06-05 (Story 2.7 → ready-for-dev via create-story: idol editor tray + grid (FR-38, UX-DR11). VISUAL + INTERACTION REBUILD ONLY — data wiring unchanged from Phase 3 (epics.md:269). Replace the current click-empty-cell → size `<select>` → inline affix picker → confirm flow with a tray + grid layout: left grid (dims/blocked-cells/shapes read from idolData.defaultGrid/IdolType — NEVER hardcode 5×4) + right scrollable Idol Tray of all idolData.idolTypes as cards (proportional rows×cols shape box + W×H label, displayName, derived affix descriptor, name filter input); Active Idol Stats summary below grid. Alec-confirmed interaction (Option A): pick idol in tray → set %s/#s (reuse IdolAffixPicker in edit mode, requiresBoth still gates) → hover grid for live size-aware placement-preview overlay (only fitting cells valid via validatePlacement()/getCellsForPlacement(); collision/overflow non-clickable) → click valid cell → placeIdol(configured); click occupied cell → clearIdolSlot; click outside grid → deselect. In-place affix edit dropped in favor of place/remove (prototype + AC2) — documented trade. NOT native HTML5 DnD (AR-8 is Epic-4 gear only) — placement is select→hover→click. Recommended structure: new IdolEditor.tsx container (lifts selectedTypeId/affix-config/hoverCell) + refactored IdolGrid.tsx (preview/place/remove) + new IdolTray.tsx; IdolTab hosts it (widen max-w-3xl). Rewrite IdolGrid.test.tsx to the tray-driven contract (drop dropdown/edit-mode tests). Source Audit: N/A — no-new-stat / no-dead-key (tray descriptor + Active Idol Stats re-present existing idolData affix data already rendered on placed cells; placed idols produced by placeIdol AND consumed by toIdolPlacements/compute_stats → no dead key; guardrail value+element loader tests N/A — no parsing change). Scope boundary: idol data wiring (placeIdol/clearIdolSlot/resetIdolGrid/updateIdolAffix/validatePlacement/getOccupantAt/idolData/PlacedIdol/toIdolPlacements/compute_stats), CenterCanvas Idols badge/routing (2.5), Blessings/Conditions (2.6), RightPanel/LeftPanel/AppHeader/StatusBar, stat sheet/optimizer/gear, and any store/dep/IPC/DnD/router are untouched.)
last_updated_create: 2026-06-05 (Story 2.6 → ready-for-dev via create-story: blessing editor card grid (FR-37). VISUAL REBUILD ONLY — data wiring unchanged from Phase 3 (epics.md:269). Rebuild BlessingsPanel.tsx from per-timeline <select>+search dropdowns into a two-column card grid (grid-cols-2): one card per monolith timeline (12 timelines × 4 blessings in blessings.json), timeline name as card header, inline selectable <button> rows (no dropdown/combobox) incl. a leading "None" row that preserves the setBlessing(timelineId, null) clear path; active blessing highlighted gold (--color-accent-gold border + -soft text) and its card gold-bordered (-gold-dim) with gold header; per-row statEffects summary re-presented (existing data — already rendered for the active blessing today). Same store reads + setBlessing(timelineId, id|null) contract; staleness banner + empty state preserved verbatim; fix pre-existing undefined var(--color-border) → --color-bg-elevated. Widen BlessingTab Blessings section (max-w-2xl → ~880px) for two columns; Conditions section + !activeBuild guard untouched. Rewrite BlessingsPanel.test.tsx to the card-grid contract (drop combobox/option + search tests; assert no combobox, card-per-timeline, inline rows + None, setBlessing calls, gold active via aria-pressed/text, statEffects text, staleness, axe). Source Audit: N/A — no-new-stat / no-dead-key (statEffects is existing shipped blessings.json data re-presented; blessings produced by setBlessing AND consumed by the snapshot serializer → no dead key; guardrail value+element loader tests N/A — no parsing change). Scope boundary: blessing data wiring (setBlessing/gameDataStore/blessings.json/snapshot/compute_stats), ConditionsPanel, CenterCanvas tab badge/routing (2.5), RightPanel/LeftPanel/AppHeader/StatusBar, stat sheet/optimizer/gear/idols, and any store/dependency/IPC/router are untouched. ContextPanel is a dead consumer (never mounted) — keep BlessingsPanel's named props-less export so it compiles.)
last_updated_create_prev_25: 2026-06-05 (Story 2.5 → ready-for-dev via create-story: center canvas six-tab bar with Weaver. FR-36 / AR-6 (partial). Extend CenterTab union += 'weaver' (passive + weaver = the two canvas tabs); six-tab bar Passive Tree | Weaver ┊ Gear | Skills | Idols | Blessings with divider, Weaver badge = Σ weaverAllocations, and safeCenterTab guard; keyboard 1–6 remap in App.tsx; thin loop-safe centerTab ⟷ SkillTreeView.activeTabIndex sync so the center Weaver tab shows the weaver tree (SkillTreeView/SkillTreeTabBar otherwise untouched — per-skill slot relocation is FR-43/Epic 5); COMPILE-REQUIRED weaver entry in getSectionStatus's exhaustive Record<CenterTab,…>; LeftPanel Weaver nav row + 'Passive Tree' relabel (the row Story 2.3 explicitly deferred here). New CenterCanvas.test.tsx, extended App.keyboard.test.tsx (1–6), buildSectionStatus.test.ts weaver boundary, reconciled LeftPanel.test.tsx. Source Audit: N/A — no-new-stat / no-dead-key (Weaver badge re-presents existing weaverAllocations; excluded from snapshot). Scope boundary: PixiJS canvas, Epic-5 skill-tree relocation, context-tab content (Epics 1/3/4), RightPanel/AppHeader/StatusBar, and all store schema/types beyond the union string are untouched.)
last_updated_prev_24: 2026-06-05 (Story 2.4 → done via code-review: all 7 ACs verified MET by Acceptance Auditor, scope boundary clean; 1 patch applied [rendered Bulwark/Aggressive custom-token zone-label assertions, RightPanel.test.tsx 30→33], 4 low-sev deferred [composite-two-ways, getArchetypeZone NaN guard, unclamped center/pill text, delta a11y text], 7 dismissed [incl. reduced-motion false-positive — global @media gate handles it]. Prior → review via dev-story: right-panel chrome rebuilt to Claude Design. ScoreGauge bars→3/4-arc SVG + ▲/▼ delta (preview wiring reused); new ScoreTrio DMG/SURV/SPD pills; OptimizationSlider UX-DR5 five-zone label via single-source getArchetypeZone (+1 token --color-zone-aggressive, reuse existing for 4/5, native accessible slider kept); FineTunePanel heading→"Fine Tune Weights"; OptimizeButton gold + pulse-gold while running (new keyframe, reduced-motion gated) + Optimize Build/Re-Optimize/Analyzing… labels; AI Suggestions/Stat Sheet header chrome. Tests updated across RightPanel/ScoreGauge/OptimizeButton + new archetypeZone unit test; RightPanel CLEARED from the failing baseline → new baseline = ProviderSelector/Settings/SkillTreeCanvas/TreeControls. tsc 0 / build 0 / full suite 1100 passed, 8 failed across exactly those 4 baseline files. No new stat (Source Audit: N/A — no-new-stat/no-dead-key). Scope boundary honored: StatSheet content (Epic 1), suggestion-card content + optimization behavior (Epic 3), all store schema/types untouched.
last_updated_create: 2026-06-05 (Story 2.4 → ready-for-dev via create-story: right-panel score-gauge/archetype/optimizer chrome rebuild. Extension of existing RightPanel.tsx + children — ScoreGauge bars→3/4-arc SVG + ▲/▼ delta (preview wiring reused), DMG/SURV/SPD pill trio, OptimizationSlider UX-DR5 five-zone label via single-source getArchetypeZone helper (+1 token --color-zone-aggressive, reuse existing for 4/5), Fine Tune heading reconciled, Optimize Build gold + pulse-gold while running (new keyframe, reduced-motion gated), AI Suggestions/Stat Sheet header chrome; native accessible slider kept (no drag-track regression). Source Audit: N/A (no new stat — chrome re-presents existing scores/sliderPosition only). Goal: clear RightPanel from the failing baseline (stale offline-copy assertion). Scope boundary: StatSheet content (Epic 1), suggestion-card content + optimization behavior (Epic 3), all store schema/types untouched.)
last_updated_prev: 2026-06-04 (Story 2.3 → done via code-review: all 6 ACs verified by Acceptance Auditor; 2 patches applied [hardened fill-count counters against missing itemName/skillName in buildSectionStatus.ts; closed LeftPanel.test.tsx coverage gaps — empty-slot filtering, V2-typed helpers, tree/idol/blessing checkmark tests, ClassGlyph branch coverage, 11→18 tests], 1 defer [hardcoded rgba gold/white literals → token pass, pre-existing], 13 dismissed; tsc 0 / build 0 / full suite 1091 passed, 9 failed across exactly the 5 documented-baseline files (LeftPanel not among them). Prior: → review via dev-story: ClassGlyph 5-class set, mastery display-name subtitle, navigator gold gate-threshold checkmarks single-sourced in shared/utils/buildSectionStatus.ts, Import Character button replaces BuildImportInput (deleted), new LeftPanel.test.tsx 11 tests + axe; tsc/build 0, no new test failures vs baseline. Originally: ready-for-dev via create-story. Extension/reconciliation of existing LeftPanel.tsx: full 5-class ClassGlyph set (Alec's call), gold gate-threshold checkmarks single-sourced for Epic 6 CompletenessGate reuse, mastery display-name subtitle, "Paste build code" → enabled Import Character button with "coming soon" toast open-intent seam (Epic 7/Story 7.1 wires the modal), gold-when-!isPersisted Save confirmed, new LeftPanel.test.tsx + axe. Source Audit: N/A (no new stat). Scope boundary: CenterTab union unchanged — no Weaver row (Story 2.5); no Character Import modal (Epic 7).)
last_updated_review_3334: 2026-06-30 (Stories 3.3 + 3.4 → done via code-review: bmad-code-review ultracode — 6-lens adversarial (Blind Hunter / Edge Case Hunter / Acceptance Auditor ×2 / Source-Audit / Render-correctness) + per-finding refutation over the combined diff 9fd6030..c34761c. 4 raw → 2 confirmed / 2 refuted. 3.4 CLEAN: rename applyNodeChangeBulk→fillNodeToMax complete (grep→0), AC2 value test exact (lvl6→budget4; root 2/maxPoints5→+2; undoStack 1), 3.5 seam untouched; ENV ANOMALY RESOLVED at HEAD — buildStore.ts budget cap Math.min(nodeSpace,budget) + useSkillTree.ts slotId===undefined guard BOTH present (bad autosave d948556 superseded by c34761c == clean tree). 3.3: 2 confirmed patches APPLIED (Alec: apply both) — ① MEDIUM compact tooltip fabricated "0 pts" + lingering highlight when a card is skipped while highlighted (React fires no onMouseLeave on unmount) → tooltip gated on a live backing suggestion + new effect clears cross-highlight/preview/tooltip when the backing suggestion is removed (SkillTreeView.tsx); ② LOW focusNode no-op on first cross-tab activation vs a freshly-mounted passive canvas → focusNode now returns a resolved-bool and SkillTreeView retries across frames until the renderer is ready (types.ts / pixiRenderer.ts / SkillTreeCanvas.tsx / SkillTreeView.tsx) + 3 pixiRenderer.test.ts boolean-contract tests. Refuted/dismissed: synthetic-card stale-highlight (dead Epic-6 scaffolding, unreachable) + pathPointCost no-anchor 0 (spec-sanctioned + test-pinned). Verified: tsc+vite build clean; full pnpm vitest 1237 passed / 8 failed = exact standing baseline (ProviderSelector/Settings/SkillTreeCanvas/TreeControls), +3 tests, no new failures. epic-3 stays in-progress — 3-5 (right-click remove-all) still backlog. ⚠️ Recommend investigating the racing formatter/agent the 3.4 dev flagged so the phantom-edit anomaly can't recur.)
project: LEBOv2
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Complete Stat Sheet & Source Attribution
  epic-1: done
  1-1-modifiertype-enum-migration-and-compute-module-split: done
  1-2-offense-stats-damage-types-crit-speed-penetration: done
  1-3-defensive-layer-computation: done
  1-4-ehp-triple-and-stable-ward-hp-equilibrium: done
  1-5-ailment-attribute-and-minion-stats: done
  1-6-five-tab-stat-sheet-panel-with-live-recompute: done
  1-7-modifiersource-tracking-and-opt-in-stat-sources: done
  1-8-stat-source-breakdown-tooltip-with-cap-gap-annotation: done
  epic-1-retrospective: done

  # Epic 2: UI/UX Revamp — Claude Design System
  epic-2: done
  2-1-design-token-reconciliation: done
  2-2-header-navigation: done
  2-3-left-panel-build-identity-and-section-navigator: done
  2-4-right-panel-score-gauge-archetype-optimizer-chrome: done
  2-5-center-canvas-six-tab-bar-with-weaver: done
  2-6-blessing-editor-card-grid: done
  2-7-idol-editor-tray-and-grid: done
  2-8-status-bar: done
  epic-2-retrospective: done  # WAIVED on-record 2026-07-01 (Epic 3 retro): all 8 stories done; UI/UX-revamp lessons captured in the 2.x last_updated_* notes — no separate retro run

  # Epic 3: Passive Tree Optimizer & Allocation
  epic-3: done
  3-1-scope-restricted-optimizer-with-empty-budget-fallback: done
  3-2-enhanced-suggested-node-visualization-on-the-canvas: done
  3-3-suggestion-card-content-and-tree-cross-highlight: done
  3-4-shift-click-fill-node-to-max: done
  3-5-right-click-remove-all-points-with-orphan-cascade: done
  epic-3-retrospective: done

  # Epic 4: Gear System & Optimization
  epic-4: in-progress
  4-0-affix-stat-semantics-data-gate: done  # added 2026-07-01 (audit correct-course): upstream re-transform giving affixes real statKey/modifierType/tiers/slots + gear_affixes ingestion + scoring parity; 4.2-4.6 gated on 4.0+4.1, mock-annotated-affixes authorization rescinded. done 2026-07-02 (bmad-code-review ultracode + fix-everything: DN-1 category mis-classification, DN-2 gate hardening, DN-3 hybrid-promotion all fixed; P-4/P-5 applied; re-ran transform 508→603 resolved 54.0%/514 unmapped; scoring-core 136 + EHP 4/4 + lebo 43 pass, coverage GATE PASSED 9/9 pins, pnpm build clean, vitest 1278/7 exact baseline)
  4-1-affix-prefix-suffix-discriminator-data-gate: review  # 2026-07-02 (dev-story): thin TS layer LANDED — position?on AffixEntryV2 + GearSlot type→position source + serializer suffix-routing (real AR-15 fix) + lib.rs GearSlotRanking/WishlistAffix re-exports. build clean; vitest 1281/7 exact baseline (+3); cargo scoring-core 136 + lebo 43 + ehp 4/4 green. Task 5 verified 4.0's corpus+Rust discriminator (no re-impl). Recommend code-review w/ a different LLM.
  4-2-item-picker-modal: backlog
  4-3-affix-picker-modal: backlog
  4-4-gear-slot-affix-pips: backlog
  4-5-three-column-gear-optimization-workspace-with-drag-drop: backlog
  4-6-ai-gear-analysis: backlog
  epic-4-retrospective: optional

  # Epic 5: Skills, Passive Trees & Popular Builds Data
  # (2026-07-02 audit correct-course, Alec decision Option A: 5-0 loader fix gates ALL
  #  authoring; 5-6..5-9 per-class passive trees (FR-50) must be done before Epic 6 planning)
  epic-5: backlog
  5-0-game-data-loader-multi-clause-stat-parsing: backlog
  5-1-complete-133-skill-database: backlog
  5-2-complete-skill-icons: backlog
  5-3-skills-tab-full-picker: backlog
  5-4-popular-builds-database-with-client-side-matching: backlog
  5-5-minion-stat-correctness: backlog
  5-6-mage-passive-tree-data-authoring: backlog
  5-7-primalist-passive-tree-data-authoring: backlog
  5-8-rogue-passive-tree-data-authoring: backlog
  5-9-acolyte-passive-tree-data-authoring: backlog
  epic-5-retrospective: optional

  # Epic 6: Complete Build Optimizer
  epic-6: backlog
  6-1-complete-build-optimizer-view-and-routing: backlog
  6-2-scope-selector: backlog
  6-3-completeness-gates-with-inline-alerts: backlog
  6-4-complete-optimization-command-scope-mask-and-stream: backlog
  6-5-unified-domain-grouped-suggestion-output: backlog
  6-6-optimization-orb-animation: backlog
  6-7-idol-ai-recommendations: backlog
  6-8-skill-suggestion-from-popular-builds: backlog
  epic-6-retrospective: optional

  # Epic 7: Character Import
  epic-7: backlog
  7-1-character-import-modal-shell-and-error-wiring: backlog
  7-2-save-file-format-spike-and-offline-detection: backlog
  7-3-offline-save-file-parsing-into-a-new-build: backlog
  7-4-online-import-stub-tab: backlog
  epic-7-retrospective: optional
